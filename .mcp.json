{
  "mcpServers": {
    "deepseek-harness": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${PLUGIN_ROOT}/scripts/server.mjs"
      ],
      "cwd": "${PLUGIN_ROOT}",
      "env": {
        "DSH_WEB_URL": "http://127.0.0.1:3080"
      }
    }
  }
}
